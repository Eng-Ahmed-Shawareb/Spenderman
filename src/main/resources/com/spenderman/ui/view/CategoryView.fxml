<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.paint.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.spenderman.ui.controller.CategoryController"
      spacing="12" styleClass="bg-root">
    <padding><Insets top="0" right="0" bottom="16" left="0"/></padding>

    <!-- Header -->
    <HBox alignment="CENTER_LEFT" spacing="12">
        <Label text="Categories" styleClass="page-title" HBox.hgrow="ALWAYS"/>
        <Button text="+ New" styleClass="btn-small" onAction="#toggleForm"/>
    </HBox>

    <!-- ═══ ADD CATEGORY FORM ═══ -->
    <VBox fx:id="formPanel" styleClass="card-form" spacing="10" visible="false" managed="false">
        <HBox alignment="CENTER_LEFT" spacing="12">
            <Label text="New Category" styleClass="section-title" HBox.hgrow="ALWAYS"/>
            <Button text="×" styleClass="btn-close" onAction="#toggleForm"/>
        </HBox>
        <HBox spacing="10">
            <VBox spacing="4" HBox.hgrow="ALWAYS" styleClass="bg-transparent">
                <Label text="NAME" styleClass="field-label"/>
                <TextField fx:id="nameField" promptText='e.g. "Food"'/>
            </VBox>
            <VBox spacing="4" HBox.hgrow="ALWAYS" styleClass="bg-transparent">
                <Label text="COLOR" styleClass="field-label"/>
                <ColorPicker fx:id="colorPicker" maxWidth="Infinity" styleClass="color-picker-field"/>
            </VBox>
        </HBox>
        <VBox spacing="4" styleClass="bg-transparent">
            <Label text="TYPE" styleClass="field-label"/>
            <HBox spacing="6" styleClass="bg-transparent">
                <ToggleButton fx:id="expenseToggle" text="Expense" styleClass="type-toggle-expense" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
                <ToggleButton fx:id="depositToggle" text="Deposit" styleClass="type-toggle-deposit" HBox.hgrow="ALWAYS" maxWidth="Infinity"/>
            </HBox>
        </VBox>
        <HBox spacing="8" alignment="CENTER_RIGHT">
            <Button text="Cancel" styleClass="btn-outline" onAction="#toggleForm"/>
            <Button text="Create Category" styleClass="btn-primary" onAction="#handleAddCategory"/>
        </HBox>
    </VBox>

    <!-- ═══ CATEGORY GRID ═══ -->
    <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS" styleClass="scroll-pane">
        <GridPane fx:id="categoryGrid" hgap="10" vgap="10"/>
    </ScrollPane>
</VBox>
